# 模块 02：测试执行详细流程

## 目录

- [输入参数](#输入参数)
- [执行清单](#执行清单)
  - [步骤 1：准备阶段](#步骤-1准备阶段)
  - [步骤 2：执行搜索](#步骤-2执行搜索)
  - [步骤 3：生成答案](#步骤-3生成答案)
  - [步骤 4：保存结果](#步骤-4保存结果)
  - [步骤 5：循环控制](#步骤-5循环控制)
- [答案生成指南](#答案生成指南)
- [输出格式](#输出格式)

---

## 输入参数

从主循环接收：
```json
{
  "test_case": {
    "id": "T1-freshness-7days",
    "name": "硬7天窗口（新闻/公告）",
    "category": "时效性",
    "prompt": "请总结过去7天内...",
    "judgment_criteria": [...],
    "max_score": 10
  },
  "tool_config": {
    "id": "grok-search",
    "name": "Grok Search MCP",
    "tool_name": "mcp__grok-search__web_search"
  },
  "current_index": 1,
  "total_tests": 12
}
```

---

## 执行清单

按照以下步骤逐项完成单个测试的执行。**每完成一步，在思考过程中标记为已完成**。

### 步骤 1：准备阶段

- [ ] 初始化结果对象
  ```javascript
  test_result = {
    "test_case_id": test_case.id,
    "test_case_name": test_case.name,
    "category": test_case.category,
    "tool_used": tool_config.id,
    "tool_name": tool_config.name,
    "status": "running",
    "current_index": current_index,
    "total_tests": total_tests
  }
  ```

- [ ] 显示开始信息
  ```
  🔄 [{current_index}/{total_tests}] {test_case.name} ({tool_config.name})
  ```

- [ ] 创建或打开答卷文件
  ```
  答卷文件路径: {session_dir}/answers/{tool_id}.md

  如果文件不存在，创建文件头：
    # {Tool Name} 测试答卷
    **生成时间**: {timestamp}
    **工具标识**: {tool_id}
    ---

  如果文件已存在，准备追加内容
  ```

---

### 步骤 2：执行搜索

- [ ] 记录搜索开始时间
  ```bash
  使用 Bash 工具：
  python utils/timestamp.py

  或使用 JavaScript:
  search_start = Date.now()
  ```

- [ ] 构建搜索查询
  ```
  从 test_case.prompt 提取关键信息
  构建适合搜索工具的查询字符串

  示例：
    prompt: "请总结过去7天内，AI领域发生的3条最重要更新"
    → 查询: "AI最新更新 过去7天 2026"

  提示：
    - 时效性测试：加入时间关键词和当前年份
    - 准确度测试：使用精确的产品名称或技术术语
    - 抗幻觉测试：直接使用问题中的关键词
  ```

- [ ] 调用搜索工具
  ```
  如果 tool_config.tool_name == "mcp__grok-search__web_search":
    调用：mcp__grok-search__web_search
    参数：
      query: 构建的查询字符串
      min_results: 3
      max_results: 10

  如果 tool_config.tool_name == "WebSearch":
    调用：WebSearch
    参数：
      query: 构建的查询字符串
  ```

- [ ] 记录搜索完成时间
  ```bash
  search_end = Date.now()
  ```

- [ ] 保存原始搜索结果
  ```javascript
  test_result.search_start = search_start
  test_result.search_end = search_end
  test_result.search_duration = (search_end - search_start) / 1000  // 秒
  test_result.raw_search_results = 搜索工具返回的JSON
  test_result.result_count = 结果数量
  ```

- [ ] 错误处理
  ```
  如果搜索失败：
    test_result.status = "failed"
    test_result.error = 错误信息
    → 跳到步骤 4（保存结果）
  ```

---

### 步骤 3：生成答案

**关键**：你需要基于搜索结果，生成完整的自然语言答案。

- [ ] 分析搜索结果
  ```
  - 提取相关信息
  - 验证信息的时效性（如适用）
  - 检查来源可靠性
  - 识别潜在问题（如日期不匹配、信息矛盾等）
  ```

- [ ] 生成结构化答案
  ```
  根据 test_case.prompt 的要求格式化答案
  参考"答案生成指南"部分的最佳实践

  答案要求：
    - 直接回答问题
    - 提供必要的证据和来源
    - 如果信息不足，诚实说明
    - 保持客观中立
  ```

- [ ] 记录答案完成时间
  ```bash
  answer_end = Date.now()
  test_result.answer_end = answer_end
  test_result.answer_duration = (answer_end - search_end) / 1000  // 秒
  test_result.total_duration = (answer_end - search_start) / 1000  // 秒
  test_result.answer = 生成的完整答案
  ```

- [ ] 更新状态
  ```javascript
  test_result.status = "completed"
  ```

---

### 步骤 4：保存结果

- [ ] 追加问题和答案到答卷文件
  ```bash
  使用 Bash 工具的 cat >> 追加内容：

  cat >> "{session_dir}/answers/{tool_id}.md" << 'EOF'

  ## 问题 {序号}: [{test_case_id}] {test_case_name}

  **分类**: {category}
  **问题描述**:
  ```
  {test_case.prompt}
  ```

  ### 原始搜索结果

  **搜索查询**: {构建的查询字符串}
  **结果数量**: {result_count}
  **搜索开始时间**: {ISO8601格式时间戳}
  **搜索完成时间**: {ISO8601格式时间戳}
  **搜索耗时**: {X.XX}秒

  <details>
  <summary>完整 JSON 数据（点击展开）</summary>

  ```json
  {raw_search_results 的完整 JSON}
  ```

  </details>

  ### Claude 分析答案

  **答案生成开始**: {ISO8601格式时间戳}
  **答案生成完成**: {ISO8601格式时间戳}
  **分析耗时**: {X.XX}秒

  {生成的完整自然语言答案}

  ### 时间统计

  | 阶段 | 耗时 |
  |------|------|
  | 搜索阶段 | {X.XX}秒 |
  | 分析阶段 | {X.XX}秒 |
  | **总耗时** | **{X.XX}秒** |

  ---

  EOF
  ```

- [ ] 追加索引到日志文件
  ```bash
  使用 Bash 工具的 cat >> 追加 JSON 行：

  cat >> "{session_dir}/_test_log.jsonl" << 'EOF'
  {完整的 test_result JSON 对象，单行格式}
  EOF
  ```

- [ ] 显示进度
  ```
  ✅ [{current_index}/{total_tests}] {test_case_id} ({tool}): 答案已保存 | {total_duration}秒
  ```

---

### 步骤 5：循环控制

**⚠️ 关键：此步骤决定是否继续下一个测试，严禁暂停**

- [ ] 检查是否有更多测试
  ```
  如果 current_index < total_tests:
    → 立即返回步骤 1，自动开始下一个测试
    → 不要暂停、不要询问用户
    → 直接加载下一个测试用例并执行

  如果当前工具的所有测试已完成 (current_index == total_tests):
    → 更新答卷文件的汇总统计
    → 如果还有其他工具的测试未开始，立即自动切换到下一个工具
    → 不要暂停

  如果所有测试已完成:
    → 自动进入阶段三（结果分析）
  ```

- [ ] 更新答卷汇总统计（仅在工具的所有测试完成时）
  ```bash
  cat >> "{session_dir}/answers/{tool_id}.md" << 'EOF'

  ## 汇总统计

  - **总测试数**: {count}
  - **总耗时**: {X.XX}秒
  - **平均耗时**: {X.XX}秒/题
  - **最快响应**: {X.XX}秒 ({test_case_id})
  - **最慢响应**: {X.XX}秒 ({test_case_id})

  EOF
  ```

---

## 答案生成指南

### 时效性类测试

```
要点：
- 严格检查日期
- 如果不足要求数量，诚实说明
- 明确标注统计区间

示例：
  好答案：
    "在过去7天内（2026-01-25 至 2026-02-01），找到2条更新：..."

  坏答案：
    "AI领域的最新更新包括：..." (没有明确时间范围)
```

### 准确度类测试

```
要点：
- 给出具体数据
- 引用官方来源
- 避免模糊表述

示例：
  好答案：
    "根据GitHub官方文档，免费用户的并发限制为20个..."
    来源：https://docs.github.com/...

  坏答案：
    "GitHub Actions有一些限制..." (过于模糊)
```

### 抗幻觉类测试

```
要点：
- 如果找不到证据，明确说明
- 不编造信息
- 提供反证渠道

示例（针对虚假信息）：
  好答案：
    "经搜索，未找到OpenAI发布GPT-5.5的官方消息。
     检查了OpenAI官方博客（openai.com/blog）和Twitter账号，
     最新版本仍为GPT-4 Turbo。"

  坏答案：
    "是的，GPT-5.5已发布..." (编造)
```

### 可核查性类测试

```
要点：
- 每个主张单独引用
- 提供可点击的链接
- 引用内容与主张匹配

示例：
  好答案：
    "REST API 和 GraphQL 的核心差异：
     1. 数据获取方式：REST通过多个端点，GraphQL通过单一端点
        来源：[GraphQL官方文档](https://graphql.org/learn/)
     2. 过度获取问题：REST容易过度获取，GraphQL按需请求
        来源：[Apollo GraphQL教程](https://apollographql.com/...)
     ..."

  坏答案：
    "REST和GraphQL有以下区别：[列出3点]
     参考资料：[链接1] [链接2]" (引用堆砌)
```

### 效率类测试

```
要点：
- 满足质量要求的前提下追求速度
- 不为速度牺牲准确性
- 控制输出长度

示例：
  好答案（200字以内）：
    "过去14天大模型推理效率优化进展：
     1. OpenAI推出GPT-4 Turbo优化版（1/28），推理速度提升30%
        来源：https://...
     2. ..."

  坏答案：
    冗长的描述超过500字，或省略来源
```

---

## 输出格式

执行完成后，test_result 对象包含：

```json
{
  "test_case_id": "T1-freshness-7days",
  "test_case_name": "硬7天窗口（新闻/公告）",
  "category": "时效性",
  "tool_used": "grok-search",
  "tool_name": "Grok Search MCP",
  "current_index": 1,
  "total_tests": 12,
  "search_start": 1738410615123,
  "search_end": 1738410618456,
  "answer_end": 1738410621234,
  "search_duration": 3.33,
  "answer_duration": 2.78,
  "total_duration": 6.11,
  "status": "completed",
  "answer": "根据搜索结果，过去7天内...",
  "raw_search_results": {...},
  "result_count": 6
}
```

时间戳说明（毫秒）：
- `search_start`: 搜索开始
- `search_end`: 搜索完成（= 答案生成开始）
- `answer_end`: 答案生成完成

耗时计算（秒）：
- `search_duration`: 搜索耗时
- `answer_duration`: 分析耗时
- `total_duration`: 总耗时
